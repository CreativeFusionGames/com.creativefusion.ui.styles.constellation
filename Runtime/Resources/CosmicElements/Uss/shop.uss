#shop-frame { max-width: 900px; }
.shop-list { row-gap: 12px; }
.shop-card { }
.shop-card__title { -unity-font-style: bold; font-style: bold; font-size: var(--font-size-md); }
.shop-card__subtitle { color: var(--ce-muted); font-size: var(--font-size-sm); }
.shop-card__row { display: flex; flex-direction: row; align-items: center; }

